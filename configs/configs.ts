export enum GamePhase {
  DAY = "day",
  DEMONS = "demons",
  DOCTOR = "doctor",
  INSPECTOR = "inspector",
}

export enum Role {
  VILLAGER = "villager",
  DEMON = "demon",
  DEMON_LEADER = "demonLeader",
  DOCTOR = "doctor",
  INSPECTOR = "inspector",
}

export enum GameState {
  PRE_LOBBY = "pre-lobby",
  PLAYING = "playing",
  LOBBY = "lobby",
  ENDED = "ended",
}
